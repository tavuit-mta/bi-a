import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuanvva3.billiardscore',
  appName: 'billiard-score',
  webDir: 'dist/billiard-score/browser',
};

export default config;
