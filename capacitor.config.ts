import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuanvva3.billiard-score',
  appName: 'billiard-score',
  webDir: 'dist/billiard-score/browser',
};

export default config;
