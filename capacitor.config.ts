import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuanvva3.counting',
  appName: 'counting',
  webDir: 'dist/counting/browser',
};

export default config;
